% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cast_df_to_sf.R
\name{cast_df_to_sf}
\alias{cast_df_to_sf}
\title{Convert a data frame with coordinates to an sf data frame}
\usage{
cast_df_to_sf(df = NULL, crs = NULL, ...)
}
\arguments{
\item{df}{(required, data.frame) Dataframe with coordinate columns (see \link{infra_x_column_names} and \link{infra_y_column_names}). Default: NULL}

\item{crs}{(optional, integer or NULL) EPSG code. If NULL (default), the CRS is guessed automatically via \code{\link[=infra_guess_crs]{infra_guess_crs()}}. Pass an explicit integer (e.g. 4326, 3857) to override
detection. Default: NULL}

\item{...}{(optional) For internal arguments only.}
}
\value{
sf data frame with POINT geometry
}
\description{
Takes a data frame or tibble with coordinates and converts it to an \code{sf} data frame of type POINT.
}
\details{
When \code{crs = NULL}, CRS detection is attempted using coordinate ranges and column names.
EPSG:4326 is returned when x ∈ [-180, 180] and y ∈ [-90, 90]. EPSG:3857 is returned
for Web Mercator scale values (x > ±1,000,000). UTM and other projected CRS cannot be
detected from coordinate values alone — pass \code{crs} explicitly in those cases.

Small local datasets in a projected CRS whose coordinate values happen to fall within
±180/±90 will be misidentified as EPSG:4326; pass \code{crs} explicitly to avoid this.
}
\seealso{
\code{\link[=infra_guess_crs]{infra_guess_crs()}}

Other casting_functions:
\code{\link[=cast_df_to_xy]{cast_df_to_xy()}},
\code{\link[=cast_sf_to_bbox]{cast_sf_to_bbox()}},
\code{\link[=cast_xy_to_xyz]{cast_xy_to_xyz()}}
}
\concept{casting_functions}
