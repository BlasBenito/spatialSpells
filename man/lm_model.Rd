% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_model.R
\name{lm_model}
\alias{lm_model}
\title{Fit Linear Model to Dataset}
\usage{
lm_model(df = NULL, response = NULL, predictors = NULL)
}
\arguments{
\item{df}{Data frame to fit the model to. If \code{NULL} (default), uses the
package's built-in \code{dummy_df} dataset. Must contain the specified
response and predictor columns.}

\item{response}{Character string specifying the name of the response variable.
If \code{NULL} (default), uses the first column of \code{df}.}

\item{predictors}{Character vector specifying the names of predictor variables.
If \code{NULL} (default), uses all columns except the response variable.}
}
\value{
An object of class \code{"lm"} containing the fitted linear model.
Use \code{\link[=summary]{summary()}}, \code{\link[=coef]{coef()}}, \code{\link[=predict]{predict()}}, and other standard methods
to extract information from the model.
}
\description{
Fits a linear regression model with flexible specification of response and
predictor variables. This is a demonstration function that works with the
\code{dummy_df} dataset included in this package or any custom data frame.
}
\details{
The function fits a linear model using \code{\link[stats:lm]{stats::lm()}}. The formula is
constructed from the specified response and predictor variables. By default,
it uses the first column as response and all other columns as predictors.

Input validation checks:
\itemize{
\item \code{df} must be a data frame or NULL
\item \code{response} must be a column name in \code{df} (if specified)
\item \code{predictors} must be column names in \code{df} (if specified)
\item Response and predictor columns must be numeric
\item At least one predictor must be specified
}
}
\examples{
# Use built-in dummy_df dataset with default settings
# (first column as response, all others as predictors)
model <- lm_model()
summary(model)

# Specify response variable explicitly
model_a <- lm_model(response = "a")
coef(model_a)

# Specify both response and predictors
model_subset <- lm_model(response = "a", predictors = c("b", "c", "d"))
summary(model_subset)

# Use only specific predictors
model_select <- lm_model(
  response = "a",
  predictors = c("b", "e", "h")
)
coef(model_select)

# Make predictions
predictions <- predict(model, newdata = dummy_df[1:10, ])
head(predictions)

# Model diagnostics
par(mfrow = c(2, 2))
plot(model)

# Use custom data with explicit variable specification
custom_data <- data.frame(
  y = rnorm(100),
  x1 = rnorm(100),
  x2 = rnorm(100),
  x3 = rnorm(100)
)
custom_model <- lm_model(
  df = custom_data,
  response = "y",
  predictors = c("x1", "x2")
)
summary(custom_model)
}
