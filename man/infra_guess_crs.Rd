% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infra_guess_crs.R
\name{infra_guess_crs}
\alias{infra_guess_crs}
\title{Guess the EPSG code from coordinate ranges and column names}
\usage{
infra_guess_crs(x, y, x_name = NULL, y_name = NULL)
}
\arguments{
\item{x}{(required, numeric) Numeric vector of x (longitude or easting) coordinates.}

\item{y}{(required, numeric) Numeric vector of y (latitude or northing) coordinates.}

\item{x_name}{(optional, character) Column name for x coordinates. Names matching the
lat/lon family (\code{lat}, \code{lon}, \code{long}, \code{longitude}, \code{latitude}, \code{latitud}, \code{longitud})
provide additional confirmation of EPSG:4326. Default: NULL}

\item{y_name}{(optional, character) Column name for y coordinates. Same name-matching
logic as \code{x_name}. Default: NULL}
}
\value{
Single integer EPSG code (4326L or 3857L), or \code{NA_integer_} with an
informative message when the CRS cannot be determined.
}
\description{
Attempts to identify the EPSG code from coordinate value ranges and column names.
Returns EPSG:4326 for geographic (lat/lon) coordinates, EPSG:3857 for Web Mercator
scale values, and \code{NA_integer_} for projected CRS that cannot be determined from
values alone (e.g. UTM).
}
\details{
Detection proceeds in four steps:
\enumerate{
\item \strong{Name heuristic}: if \code{x_name} or \code{y_name} matches the lat/lon family,
this is recorded as additional confirmation of EPSG:4326.
\item \strong{EPSG:4326 range check}: if \code{max(abs(x)) <= 180} AND \code{max(abs(y)) <= 90},
returns 4326L. A message notes whether the column names also confirmed this.
\item \strong{EPSG:3857 range check}: if \code{max(abs(x)) > 1000000} AND
\code{max(abs(x)) <= 20037509} AND \code{max(abs(y)) <= 20048967}, returns 3857L.
\item \strong{Fallback}: returns \code{NA_integer_} with a message asking the user to
supply \code{crs} explicitly.
}

\strong{Limitations}:
\itemize{
\item Small spatial extents in a projected CRS whose coordinate values happen to fall
within ±180/±90 will be misclassified as EPSG:4326. Pass \code{crs} explicitly for
such cases.
\item UTM and other national or regional projected CRS cannot be detected from coordinate
values alone. Pass \code{crs} explicitly.
\item Web Mercator datasets with x extents below ±1,000,000 (e.g., a small local area
near the prime meridian) will not be detected as EPSG:3857. Pass \code{crs} explicitly.
}
}
\examples{
# EPSG:4326 from coordinate ranges
infra_guess_crs(x = c(-10, 0, 10), y = c(35, 40, 45))

# EPSG:4326 confirmed by column names
infra_guess_crs(x = c(1, 2, 3), y = c(40, 41, 42), x_name = "lon", y_name = "lat")

# EPSG:3857 from Web Mercator scale values
infra_guess_crs(x = c(-8000000, 0, 8000000), y = c(-5000000, 0, 5000000))

# NA for UTM or other projected CRS
infra_guess_crs(x = c(500000, 510000), y = c(4500000, 4510000))
}
\seealso{
Other infra:
\code{\link[=infra_call_stack]{infra_call_stack()}},
\code{\link[=infra_get_wkt_column]{infra_get_wkt_column()}},
\code{\link[=infra_get_x_column]{infra_get_x_column()}},
\code{\link[=infra_get_xy_columns]{infra_get_xy_columns()}},
\code{\link[=infra_get_y_column]{infra_get_y_column()}},
\code{\link[=infra_validate_df]{infra_validate_df()}},
\code{\link{infra_x_column_names}},
\code{\link{infra_y_column_names}}
}
\concept{infra}
